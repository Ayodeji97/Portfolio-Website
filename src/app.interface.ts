export interface TechnologyIcon {
  src: string;
  alt: string;
}

export interface ExperienceType {
  role: string;
  companyName: string;
  description: string;
}

export interface ProjectType {
  title: string;
  description: string;
  githubLink: string;
  liveLink: string;
  img: string;
}
